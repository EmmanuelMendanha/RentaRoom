{% extends 'base.html.twig' %}


{% block body %}
    <h1>Edit Booking</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}

        <button type="submit" class="btn btn-primary" onclick="return confirm('Are you sure you want to update this booking?')">Save changes</button>
    {{ form_end(form) }}



{% endblock %}