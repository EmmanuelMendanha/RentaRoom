{% extends 'base.html.twig' %}


{% block body %}
    <h1>Delete Booking</h1>

    <p>Are you sure you want to delete this booking?</p>

    <a href="{{ path('booking_delete', {id: booking.id}) }}" class="btn btn-danger">Yes, delete it</a>
    <a href="{{ path('app_booking') }}" class="btn btn-secondary">No, go back</a>
{% endblock %}